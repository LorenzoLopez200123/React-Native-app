import React from 'react'
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native'

export default function CartStack({ navigator }) {
  return (
    <View>
      <Text>Cart</Text>
    </View>
  )
}
